# Site images

Drop your photos here (e.g. `.jpg`, `.png`, `.webp`). Reference them in HTML like:

```html
<img src="images/your-photo.jpg" alt="Description">
```

Use lowercase names and hyphens instead of spaces (e.g. `harley-build-01.jpg`).
